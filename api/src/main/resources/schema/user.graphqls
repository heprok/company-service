type User {
    id: ID!
    firstName: String!
    lastName: String!
    slug: String!
    jobPosition: String
    image: Image
}

type UserList implements PaginatedList {
    items: [User!]!
    totalItems: Int!
}
